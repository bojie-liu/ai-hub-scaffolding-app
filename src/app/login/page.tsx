import { LoginForm } from '@/components/auth/LoginForm';

export const metadata = {
  title: 'Login - Knowledge Management & School Development',
};

export default function LoginPage() {
  return <LoginForm />;
}
